[project]
name = "sql-lineage"
version = "1.0.1"
description = "Deterministic SQL analysis using sqlglot AST parsing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "sqlglot[rs]>=26.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
